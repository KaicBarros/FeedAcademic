@model IEnumerable<Model.Models.Turma>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Codigo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NomeTurma)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmailProfessor)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Codigo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NomeTurma)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EmailProfessor)
        </td>
        <td>
            @Html.ActionLink("Editar", "EditTurma", new { id=item.Codigo }) |
            @Html.ActionLink("Details", "DetailsTurma", new { id = item.Codigo }) |
            @Html.ActionLink("Excluir", "DeleteTurma", new { id = item.Codigo })
        </td>
    </tr>
}

</table>
<div class="col-md-2"  >
    @Html.ActionLink("Nova Turma", "CreateTurma")
</div>